@model IEnumerable<CRUD_Dapper1.Models.Pessoas>

@{
    ViewData["Title"] = "Listagem de Pessoas ";
}

<h4>Listagem de pessoas</h4>

<p>
    <a asp-action="Create" class="btn btn-outline-success btn-sm">Nova pessoa</a>
</p>
<table class="table table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PessoaID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.idade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.peso)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PessoaID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.idade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.peso)
            </td>
            <td>
                <a asp-asp-controller="Pessoas" asp-action="Edit" asp-route-pessoaid="@item.PessoaID" class="btn btn-outline-info btn-sm" >Atualizar</a>&emsp;
                <form asp-asp-controller="Pessoas" asp-action="Delete" asp-route-pessoaid="@item.PessoaID" onclick="confirm('Prosseguir com a exclucao do usuario @item.Nome ?')">
                        <button type="submit" class="btn btn-outline-danger btn-sm">Excluir</button>
                    </form>
            </td>
        </tr>
}
    </tbody>
</table>
